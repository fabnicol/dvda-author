if test -d "$PWD/@MAYBE_SOX@"
then
  cp "@ROOTDIR@/sox-libs" "$PWD/@MAYBE_SOX@/sox-libs.in" 
else
  echo "$PWD/@MAYBE_SOX@ is not a directory"
  exit -1
fi 
cd "@MAYBE_SOX@" 

./configure @CONFIGURE_SOX_FLAGS@ --without-oggvorbis --without-mad && make -j4 -k && make install; cd - 
